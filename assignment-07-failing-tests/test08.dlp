// Error Cases for Missing Return Statements
int incomplete() {
    var x: int = 5;
    if (x > 0) {
        return x;
    }
    // Missing return statement for else case
}

int main() {
    return incomplete();
}